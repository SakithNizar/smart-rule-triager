{
  "private": true,
  "name": "smart-rule-triag",
  "version": "0.0.0",
  "license": "BSD-3-Clause",
  "type": "module",
  "scripts": {
    "build": "node --experimental-strip-types ./tools/build.ts --minify",
    "deploy": "npm run build && devvit upload",
    "dev": "devvit playtest",
    "login": "devvit login",
    "launch": "npm run build && npm run deploy && devvit publish",
    "type-check": "tsc --build"
  },
  "engines": {
    "node": ">=22.6.0"
  },
  "dependencies": {
    "@devvit/web": "0.12.22",
    "devvit": "0.12.24"
  },
  "devDependencies": {
    "@types/node": "25.6.2",
    "typescript": "6.0.3",
    "esbuild": "0.28.0"
  }
}